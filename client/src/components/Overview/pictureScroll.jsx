import React from 'react';
// import carousel from 'react-multi-carousel'; //see the link below for more details on this

//the carousel that appears superimposed over the main picture
//should be scrollable
//on click, picture should render as the main picture
//setting these up as a series of clickable buttons

//this should show what needs to be done https://www.npmjs.com/package/react-multi-carousel Description: "Production-ready, lightweight fully customizable React carousel component that rocks supports multiple items and SSR(Server-side rendering)."

const pictureScroll = (props) => {
  //carousel of alternate images of product
}
export default pictureScroll;