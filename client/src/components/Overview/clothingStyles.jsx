import React from 'react';

//color/pattern clickable buttons.
//needs to only display the amount of colors/patterns available for each particular garment

const clothingStyles = (props) => {
  //change clothing style
}

export default clothingStyles;